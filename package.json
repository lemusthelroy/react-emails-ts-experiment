{
  "name": "react-emails-ts",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^13.0.0",
    "@testing-library/user-event": "^13.2.1",
    "@types/jest": "^27.0.1",
    "@types/node": "^16.7.13",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "node-fetch": "^3.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.4.2",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "nuke": "rm -Rf node_modules && rm -Rf .netlify/functions-internal && rm -Rf .netlify/functions && yarn && netlify build && netlify dev",
    "nuke-partial": "rm -Rf node_modules && rm -Rf .netlify/functions-internal && rm -Rf .netlify/functions && rm -Rf .netlify/functions-serve && yarn && netlify build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@sendgrid/mail": "^7.7.0",
    "cheerio": "^1.0.0-rc.12",
    "form-data": "^4.0.0",
    "handlebars": "^4.7.7",
    "mailgun.js": "^8.0.2",
    "mjml": "^4.13.0",
    "postmark": "^3.0.14"
  }
}
